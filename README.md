# incremental-socket-fuzzer
A Python socket fuzzer that slowly increases payload size until the service can’t handle the pressure anymore.  Built to observe crashes, not to exploit them.  Calm, patient, and absolutely relentless.
